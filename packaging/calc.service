[Unit]
Description=Calc TCP Service
After=network.target postgresql.service
Requires=postgresql.service

[Service]
Type=simple
ExecStart=/usr/bin/calc
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target